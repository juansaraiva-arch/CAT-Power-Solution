# CAT Power Solution — Streamlit App Dependencies
# =================================================
# Only what the Streamlit demo needs.
# For the full FastAPI server, see requirements-api.txt.

# Core (calculation engine)
numpy>=1.26.0
reportlab>=4.1.0
pdfplumber>=0.11.0

# Schemas (used by sizing pipeline)
pydantic>=2.6.0

# Streamlit framework
streamlit>=1.32.0

# UI / Visualization
plotly>=5.18.0
pandas>=2.1.0

# Document generation (proposal DOCX)
python-docx>=1.1

# Authentication (Streamlit auth flow)
bcrypt>=4.1.0
